{
  "name": "@cindy/design-tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Cindy DTCG design sources and build-time Desktop generation.",
  "type": "module",
  "scripts": {
    "generate": "node --experimental-strip-types src/generate.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check:generated": "node --experimental-strip-types src/generate.ts --check"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@terrazzo/cli": "2.7.1",
    "@terrazzo/parser": "2.7.1",
    "@types/node": "*",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
